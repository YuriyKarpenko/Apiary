<UserControl x:Class="Apiary.V.V_Family"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 
			 xmlns:core="clr-namespace:Apiary.Data;assembly=Apiary.Data" 
			 xmlns:v="clr-namespace:Apiary.V" 
			 xmlns:vm="clr-namespace:Apiary.VM" 
             
			 mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid.Resources>
			<DataTemplate DataType="{x:Type core:IM_Family}">
				<StackPanel>
					<TextBlock Text="{Binding Name}"/>
				</StackPanel>
			</DataTemplate>
		</Grid.Resources>

		<StackPanel Orientation="Horizontal">
			<TextBlock Text="Выбор семьи : "/>
			<ComboBox ItemsSource="{Binding List.List}" SelectedItem="{Binding List.SelectedItem}" MinWidth="200"/>
		</StackPanel>
		
	</Grid>
</UserControl>
